다년 누적 영업이익 vs 영업현금흐름의 괴리를 계산하는 결정론적 게이트. LLM 호출을 넣지 않는다. 파싱·발굴·정규화 로직도 넣지 않는다.

`ebitda_run.py` — 정상화 기준선 EBITDA 조립(경계 I/O). 영업이익(재무제표) + D&A(주석, `extract/da_notes`) = EBITDA. 결정론적·LLM 없음. 2단 경로(성격별→개별주석 합산), A/B/C 계상위치 판정, 리스 별도줄, 교차검증. 모든 숫자에 주석 출처. 임계·색·등급 없음.
