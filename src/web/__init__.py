"""웹 껍데기 (로컬 Flask). 기존 파이프라인(screen·surface·D&A·normalize)을 그대로 호출만 한다.

이 패키지는 재계산 공식·게이트·탐지 로직을 절대 재구현하지 않는다. CLI 진입점을
서브프로세스로 부르거나(대부분), surface 는 진행 표시(3회 중 N회)를 위해 web 계층에서
run_once 를 관찰만 하는 얇은 래퍼로 감싼다(계산은 discover.main 이 그대로 수행).
API 키는 화면에서 입력받아 메모리에만 두고, 서브프로세스 env 로만 전달한다(파일·로그 저장 금지).
"""
